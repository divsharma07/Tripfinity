<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.tripfinity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tripfinity_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tripfinity_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tripfinity">
        <activity
            android:name=".view.AddPlaceActivity"
            android:exported="false" />
        <activity
            android:name=".view.ItineraryViewActivity"
            android:exported="false" />
        <activity
            android:name=".view.TripCreationActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".view.SplashScreenActivity" -->
        <!-- android:exported="true" -->
        <!-- android:theme="@style/SplashTheme"> -->
        <!-- &lt;!&ndash;            <intent-filter>&ndash;&gt; -->
        <!-- &lt;!&ndash;                <action android:name="android.intent.action.MAIN" />&ndash;&gt; -->
        <!-- &lt;!&ndash;                <category android:name="android.intent.category.LAUNCHER" />&ndash;&gt; -->
        <!-- &lt;!&ndash;            </intent-filter>&ndash;&gt; -->
        <!-- </activity> -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>